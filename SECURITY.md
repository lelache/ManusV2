# SECURITY — ManusV2

- Signaler toute vulnérabilité via issues privées ou email du maintainer.
- Pas de secrets en clair dans le repo (`.env` ignoré).
- Branches protégées : `main` (PR requis), `develop` (PR recommandé).
- Revue obligatoire pour changements sensibles (auth, secrets, DB).
- Mises à jour de dépendances régulières (Dependabot/GitHub Alerts).
