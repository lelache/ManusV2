name: CI

on:
  push:
    branches: [develop, main]
    paths:
      - 'frontend/**'
      - '.github/workflows/**'
  pull_request:
    branches: [develop, main]
    paths:
      - 'frontend/**'
      - '.github/workflows/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PNPM
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Show workspace
        run: |
          pwd
          ls -la
          test -f pnpm-lock.yaml && echo "pnpm-lock.yaml OK" || (echo "pnpm-lock.yaml MISSING" && exit 1)

      - name: Install dependencies
        run: pnpm install

      - name: Lint
        run: pnpm lint || echo "Lint skipped (no config yet)"

      - name: Build
        env:
          NODE_ENV: production
        run: pnpm build
